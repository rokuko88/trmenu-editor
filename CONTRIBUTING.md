# 贡献指南

感谢你对 TRMenu Editor 项目的贡献！本文档将帮助你了解项目的开发流程和规范。

## 开发环境设置

### 1. 克隆项目

```bash
git clone https://github.com/your-repo/trmenu-editor.git
cd trmenu-editor
```

### 2. 安装依赖

```bash
npm install
```

### 3. 初始化 Husky（Git Hooks）

```bash
npm run prepare
```

### 4. 启动开发服务器

```bash
npm run dev
```

访问 [http://localhost:3000](http://localhost:3000) 查看应用。

## 开发规范

### 代码风格

项目使用以下工具确保代码质量和一致性：

- **ESLint**: JavaScript/TypeScript 代码检查
- **Prettier**: 代码格式化
- **EditorConfig**: 编辑器配置统一

### 提交规范

本项目遵循 [Conventional Commits](https://www.conventionalcommits.org/) 规范。

#### 提交类型

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行的变动）
- `refactor`: 重构（既不是新增功能，也不是修改 bug 的代码变动）
- `perf`: 性能优化
- `test`: 添加测试
- `build`: 构建系统或外部依赖的变动
- `ci`: CI 配置文件和脚本的变动
- `chore`: 其他不修改 src 或测试文件的变动
- `revert`: 回滚 commit

#### 提交消息格式

```
<type>(<scope>): <subject>

<body>

<footer>
```

**示例：**

```bash
feat(color-picker): 添加 Minecraft 颜色选择器组件

- 支持传统颜色代码（§0-§f）
- 支持六进制颜色
- 支持渐变色效果
```

### Git Hooks

项目配置了以下 Git Hooks：

#### Pre-commit

在提交前自动执行：

- 运行 ESLint 检查并自动修复
- 运行 Prettier 格式化代码
- 仅处理 staged 的文件

#### Commit-msg

检查提交消息是否符合规范。

## 可用的 NPM Scripts

### 开发

```bash
# 启动开发服务器
npm run dev

# 类型检查
npm run type-check

# ESLint 检查
npm run lint

# ESLint 自动修复
npm run lint:fix

# Prettier 格式化
npm run format

# 检查代码格式
npm run format:check

# 运行所有检查（类型检查 + ESLint + Prettier）
npm run check-all
```

### 构建

```bash
# 生产环境构建
npm run build

# 启动生产服务器
npm run start
```

## 推荐的 VS Code 扩展

项目已配置推荐扩展列表（`.vscode/extensions.json`）：

- **ESLint**: 代码检查
- **Prettier**: 代码格式化
- **Tailwind CSS IntelliSense**: Tailwind CSS 智能提示
- **EditorConfig**: 编辑器配置

首次打开项目时，VS Code 会提示安装这些扩展。

## 代码审查清单

在提交 PR 前，请确保：

- [ ] 代码通过了所有 ESLint 检查
- [ ] 代码已使用 Prettier 格式化
- [ ] TypeScript 类型检查通过
- [ ] 提交消息符合规范
- [ ] 更新了相关文档（如有需要）
- [ ] 测试通过（如有测试）

## 项目结构

```
trmenu-editor/
├── .husky/              # Git hooks 配置
├── .vscode/             # VS Code 工作区配置
├── public/              # 静态资源
├── src/
│   ├── app/            # Next.js 应用路由
│   ├── components/     # React 组件
│   │   ├── ui/        # 基础 UI 组件（shadcn/ui）
│   │   └── menu-editor/ # 菜单编辑器组件
│   ├── lib/           # 工具函数
│   ├── store/         # Zustand 状态管理
│   └── types/         # TypeScript 类型定义
├── .editorconfig       # 编辑器配置
├── .prettierrc         # Prettier 配置
├── .lintstagedrc.js    # lint-staged 配置
├── commitlint.config.js # commitlint 配置
└── package.json        # 项目配置
```

## 常见问题

### Q: 为什么我的提交被拒绝？

A: 可能的原因：

1. 代码不符合 ESLint 规则 → 运行 `npm run lint:fix`
2. 代码格式不正确 → 运行 `npm run format`
3. 提交消息不符合规范 → 检查提交消息格式

### Q: 如何跳过 Git Hooks？

A: 在特殊情况下（不推荐），可以使用：

```bash
git commit --no-verify
```

### Q: 类型检查失败怎么办？

A: 运行 `npm run type-check` 查看详细错误信息，修复后重新提交。

## 获取帮助

如有问题，欢迎：

- 提交 Issue
- 加入讨论
- 联系维护者

感谢你的贡献！🎉
